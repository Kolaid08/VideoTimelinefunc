<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
    <title>@ViewBag.Title – 🎬 Video Timeline Marker</title>
    <link rel="stylesheet" href="~/Content/Site.css" />
</head>
<body>
    <nav class="app-nav">
        <div class="nav-inner">
            <a href="@Url.Action("Index","Home")" class="nav-brand">🎬 Video Timeline Marker</a>
            <div class="nav-links">
                <a href="@Url.Action("Index","Home")" class="nav-link">📚 Thư viện video</a>
            </div>
        </div>
    </nav>

    @if (TempData["Success"] != null)
    {
        <div class="flash flash-ok">✅ @TempData["Success"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="flash flash-err">❌ @TempData["Error"]</div>
    }

    <main class="main-content">
        @RenderBody()
    </main>

    @RenderSection("Scripts", required: false)
</body>
</html>
